# 홈페이지에 설치 파일 올리기 가이드

## 📦 현재 상태

- ✅ 빌드 완료: `release/RefBoard Setup 0.0.1.exe` (또는 0.0.0.exe)
- ✅ 홈페이지: `website/index.html`에 다운로드 링크 준비됨
- 📍 목표: `https://trackk81.github.io/refboard-website/`에서 설치 파일 다운로드 가능하게 하기

---

## 🚀 배포 단계

### 1단계: 설치 파일 확인

빌드된 설치 파일 위치:
```
D:\Scripts\RefBoard\release\RefBoard Setup 0.0.1.exe
```

파일명을 확인하세요. 버전에 따라 다를 수 있습니다:
- `RefBoard Setup 0.0.0.exe`
- `RefBoard Setup 0.0.1.exe`

---

### 2단계: refboard-website 리포지토리 위치 확인

`refboard-website` 리포지토리가 어디에 있는지 확인하세요.

**일반적인 위치:**
- `D:\Scripts\refboard-website`
- `D:\Projects\refboard-website`
- 또는 다른 위치

---

### 3단계: downloads 폴더 생성 및 파일 복사

#### 방법 A: Windows 탐색기 사용 (가장 쉬움) ⭐

1. **두 개의 탐색기 창 열기**
   - 창 1: `D:\Scripts\RefBoard\release` 폴더
   - 창 2: `refboard-website` 리포지토리 폴더

2. **downloads 폴더 생성**
   - `refboard-website` 폴더 안에 `downloads` 폴더가 없으면 생성
   - 있으면 그대로 사용

3. **설치 파일 복사**
   - `release` 폴더에서 `RefBoard Setup 0.0.1.exe` 파일 찾기
   - 파일을 복사 (`Ctrl + C`)
   - `refboard-website/downloads/` 폴더에 붙여넣기 (`Ctrl + V`)

4. **파일명 확인**
   - 복사된 파일명이 `RefBoard Setup 0.0.1.exe`인지 확인
   - 또는 `RefBoard Setup 0.0.0.exe`인지 확인

#### 방법 B: CMD 사용

```cmd
cd D:\Scripts\refboard-website
mkdir downloads
copy "D:\Scripts\RefBoard\release\RefBoard Setup 0.0.1.exe" downloads\
```

**파일명이 다른 경우:**
```cmd
copy "D:\Scripts\RefBoard\release\RefBoard Setup 0.0.0.exe" downloads\
```

---

### 4단계: website/index.html 파일명 확인 및 수정

현재 `website/index.html`에는 다음 링크가 있습니다:
```html
<a href="downloads/RefBoard-Setup-0.0.0.exe" class="btn-primary" download>
```

**실제 설치 파일명에 맞게 수정:**

#### 파일명이 `RefBoard Setup 0.0.1.exe`인 경우:

파일명에 공백이 있으면 URL 인코딩이 필요합니다:
- `RefBoard Setup 0.0.1.exe` → `RefBoard%20Setup%200.0.1.exe`

또는 파일명을 변경하는 것이 더 간단합니다:

1. **파일명 변경 (권장)**
   - `downloads` 폴더에서 파일명을 `RefBoard-Setup-0.0.1.exe`로 변경
   - 공백을 하이픈(`-`)으로 변경

2. **index.html 수정**
   ```html
   <a href="downloads/RefBoard-Setup-0.0.1.exe" class="btn-primary" download>
   ```

#### 파일명이 `RefBoard Setup 0.0.0.exe`인 경우:

현재 설정이 맞습니다. 수정 불필요.

---

### 5단계: 버전 정보 업데이트 (선택사항)

`website/index.html`의 버전 표시도 업데이트할 수 있습니다:

```html
<div class="hero-badge">Ver 0.0.1 Now Available</div>
```

---

### 6단계: Git 커밋 및 푸시

1. **refboard-website 리포지토리로 이동**
   ```cmd
   cd D:\Scripts\refboard-website
   ```
   (실제 경로로 변경)

2. **변경사항 확인**
   ```cmd
   git status
   ```
   
   **보이는 내용:**
   ```
   On branch main
   Changes not staged for commit:
     modified:   index.html
   Untracked files:
     downloads/RefBoard-Setup-0.0.1.exe
   ```

3. **파일 추가**
   ```cmd
   git add downloads/
   git add index.html
   ```
   
   또는 모든 변경사항 추가:
   ```cmd
   git add .
   ```

4. **커밋**
   ```cmd
   git commit -m "Add RefBoard installer v0.0.1"
   ```

5. **푸시**
   ```cmd
   git push origin main
   ```

---

### 7단계: 배포 확인

1. **GitHub에서 확인**
   - 브라우저에서 `https://github.com/trackk81/refboard-website` 접속
   - `downloads` 폴더가 보이는지 확인
   - `downloads/RefBoard-Setup-0.0.1.exe` 파일이 있는지 확인

2. **홈페이지에서 다운로드 테스트**
   - `https://trackk81.github.io/refboard-website/` 접속
   - "Windows용 다운로드" 버튼 클릭
   - 파일이 다운로드되는지 확인

---

## ✅ 체크리스트

- [ ] 설치 파일 위치 확인 (`release/` 폴더)
- [ ] 설치 파일명 확인 (`RefBoard Setup 0.0.1.exe` 또는 `0.0.0.exe`)
- [ ] `refboard-website` 리포지토리 위치 확인
- [ ] `downloads` 폴더 생성
- [ ] 설치 파일을 `downloads/` 폴더로 복사
- [ ] 파일명을 URL 친화적으로 변경 (공백 → 하이픈)
- [ ] `website/index.html`의 링크 수정
- [ ] 버전 정보 업데이트 (선택사항)
- [ ] Git 커밋 및 푸시
- [ ] 홈페이지에서 다운로드 테스트

---

## 🔧 문제 해결

### 파일명에 공백이 있는 경우

**문제:** `RefBoard Setup 0.0.1.exe`는 URL에서 공백이 `%20`으로 인코딩됩니다.

**해결:** 파일명을 변경하는 것이 가장 간단합니다:
- `RefBoard Setup 0.0.1.exe` → `RefBoard-Setup-0.0.1.exe`

### 다운로드가 안 되는 경우

1. **파일 경로 확인**
   - `downloads/RefBoard-Setup-0.0.1.exe` 경로가 올바른지 확인
   - 대소문자 구분 확인

2. **GitHub Pages 캐시**
   - 몇 분 기다린 후 다시 시도
   - 브라우저 캐시 삭제 후 재시도

3. **직접 URL 접근**
   - `https://trackk81.github.io/refboard-website/downloads/RefBoard-Setup-0.0.1.exe` 직접 접속
   - 파일이 다운로드되면 링크는 정상입니다

---

## 📝 빠른 참조

### 전체 명령어 (한 번에 실행)

```cmd
cd D:\Scripts\refboard-website
mkdir downloads
copy "D:\Scripts\RefBoard\release\RefBoard Setup 0.0.1.exe" downloads\RefBoard-Setup-0.0.1.exe
git add downloads/
git add index.html
git commit -m "Add RefBoard installer v0.0.1"
git push origin main
```

---

## 🎉 완료!

배포가 완료되면 사용자들이 홈페이지에서 설치 파일을 다운로드할 수 있습니다!

