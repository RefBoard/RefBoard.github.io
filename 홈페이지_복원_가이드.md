# 홈페이지 복원 가이드

## 🔍 문제 상황

RefBoard 앱을 루트에 배포하면서 기존 홈페이지 `index.html`이 덮어씌워졌습니다.

**현재 상태:**
- `https://trackk81.github.io/refboard-website/index.html` → RefBoard 앱이 표시됨
- 원래 홈페이지가 사라짐

**목표:**
- `https://trackk81.github.io/refboard-website/index.html` → 원래 홈페이지 표시
- `https://trackk81.github.io/refboard-website/refboard/` → RefBoard 앱 접근

---

## ✅ 해결 방법

### 1단계: 설정 변경 완료 ✅

`vite.config.web.ts`의 `base`를 `/refboard/`로 변경했습니다.

### 2단계: 홈페이지 파일 복원

`refboard-website` 리포지토리의 루트에 다음 파일들을 복원해야 합니다:

#### 필요한 파일들:
1. **`index.html`** - 홈페이지 메인 (RefBoard 소개 페이지)
2. **`style.css`** - 홈페이지 스타일
3. **`privacy.html`** - 개인정보처리방침
4. **`terms.html`** - 이용약관 (있는 경우)

#### 파일 위치:
- 원본 파일: `D:\Scripts\RefBoard\website\` 폴더에 있습니다
- 배포 위치: `D:\Scripts\refboard-website\` 리포지토리 루트

---

## 📋 배포 단계

### 방법 1: Windows 탐색기 사용 (추천) ⭐

1. **원본 파일 복사**
   - `D:\Scripts\RefBoard\website\` 폴더 열기
   - 다음 파일들을 선택:
     - `index.html`
     - `style.css`
     - `privacy.html`
     - `terms.html` (있는 경우)
   - `Ctrl + C`로 복사

2. **리포지토리로 이동**
   - `D:\Scripts\refboard-website\` 폴더 열기

3. **파일 붙여넣기**
   - 루트 폴더에 `Ctrl + V`로 붙여넣기
   - 기존 파일이 있으면 덮어쓰기 확인

### 방법 2: CMD 사용

```bash
# refboard-website 리포지토리로 이동
cd D:\Scripts\refboard-website

# 원본 파일 복사
copy D:\Scripts\RefBoard\website\index.html index.html
copy D:\Scripts\RefBoard\website\style.css style.css
copy D:\Scripts\RefBoard\website\privacy.html privacy.html
```

---

## 🔨 3단계: RefBoard 앱 재빌드 및 배포

### 1. RefBoard 앱 재빌드

CMD에서:
```bash
cd D:\Scripts\RefBoard
npm run build:web
```

### 2. 빌드 파일을 `/refboard/` 폴더에 복사

**Windows 탐색기 사용:**
1. `D:\Scripts\RefBoard\dist-web\` 폴더 열기
2. 모든 파일 선택 (`Ctrl + A`)
3. 복사 (`Ctrl + C`)
4. `D:\Scripts\refboard-website\refboard\` 폴더 열기
5. 폴더가 없으면 생성
6. 붙여넣기 (`Ctrl + V`)

**CMD 사용:**
```bash
# refboard 폴더 생성 (없는 경우)
mkdir D:\Scripts\refboard-website\refboard

# 파일 복사
xcopy /E /I /Y D:\Scripts\RefBoard\dist-web\* D:\Scripts\refboard-website\refboard\
```

---

## 📤 4단계: Git 커밋 및 푸시

```bash
# refboard-website 리포지토리로 이동
cd D:\Scripts\refboard-website

# 변경사항 확인
git status

# 모든 변경사항 추가
git add .

# 커밋
git commit -m "Restore homepage and deploy RefBoard to /refboard folder"

# 푸시
git push origin main
```

---

## ✅ 완료 확인

배포 완료 후 (약 1-2분 소요):

1. **홈페이지 확인**
   - `https://trackk81.github.io/refboard-website/index.html`
   - 원래 홈페이지가 표시되어야 합니다
   - "RefBoard 시작하기" 버튼이 있어야 합니다

2. **RefBoard 앱 확인**
   - `https://trackk81.github.io/refboard-website/refboard/`
   - RefBoard 앱이 정상적으로 로드되어야 합니다

---

## 🎯 최종 구조

```
refboard-website/
├── index.html          ← 홈페이지 메인 (복원됨)
├── style.css           ← 홈페이지 스타일 (복원됨)
├── privacy.html        ← 개인정보처리방침 (복원됨)
├── terms.html          ← 이용약관 (있는 경우)
└── refboard/           ← RefBoard 앱
    ├── index.html
    ├── assets/
    └── ...
```

---

## 🆘 문제 해결

### 문제 1: 홈페이지가 여전히 RefBoard 앱으로 보임

**원인:** 브라우저 캐시 문제일 수 있습니다.

**해결:**
- `Ctrl + Shift + R` (강력 새로고침)
- 또는 브라우저 캐시 삭제

### 문제 2: RefBoard 앱이 404 에러

**원인:** `refboard` 폴더에 파일이 없거나 경로가 잘못되었을 수 있습니다.

**해결:**
- `refboard` 폴더에 파일이 있는지 확인
- `vite.config.web.ts`의 `base`가 `/refboard/`로 설정되어 있는지 확인

### 문제 3: CSS가 로드되지 않음

**원인:** `style.css` 파일 경로 문제일 수 있습니다.

**해결:**
- `index.html`에서 `style.css` 경로가 올바른지 확인
- 파일이 루트에 있는지 확인

---

## 📝 체크리스트

- [ ] `vite.config.web.ts`의 `base`가 `/refboard/`로 설정됨
- [ ] 홈페이지 파일들 (`index.html`, `style.css`, `privacy.html`) 복원됨
- [ ] RefBoard 앱 재빌드 완료
- [ ] `refboard` 폴더에 빌드 파일 복사 완료
- [ ] Git 커밋 및 푸시 완료
- [ ] 홈페이지 확인 (`https://trackk81.github.io/refboard-website/index.html`)
- [ ] RefBoard 앱 확인 (`https://trackk81.github.io/refboard-website/refboard/`)

---

## 🎉 완료!

모든 단계를 완료하면:
- 홈페이지가 정상적으로 표시됩니다
- RefBoard 앱은 `/refboard/` 경로에서 접근 가능합니다
- 홈페이지에서 "RefBoard 시작하기" 버튼으로 앱에 접근할 수 있습니다



