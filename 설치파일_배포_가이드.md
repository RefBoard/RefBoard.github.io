# Windows 설치 파일 배포 가이드

## 📋 목표

홈페이지에서 Windows 설치 파일(`RefBoard Setup 0.0.0.exe`)을 다운로드할 수 있도록 설정합니다.

---

## ✅ 1단계: 홈페이지 수정 완료

홈페이지가 설치 파일 다운로드 링크를 포함하도록 수정했습니다:
- "RefBoard 시작하기" 버튼 → "Windows 설치 파일 다운로드" 버튼으로 변경
- 다운로드 아이콘 추가

---

## 📦 2단계: 설치 파일 복사

### Windows 탐색기 사용 (추천) ⭐

1. **설치 파일 찾기**
   - `D:\Scripts\RefBoard\release\RefBoard Setup 0.0.0.exe` 파일 열기

2. **다운로드 폴더 생성**
   - `D:\Scripts\refboard-website\downloads\` 폴더 생성 (없는 경우)

3. **파일 복사**
   - `RefBoard Setup 0.0.0.exe` 파일 복사 (`Ctrl + C`)
   - `D:\Scripts\refboard-website\downloads\` 폴더에 붙여넣기 (`Ctrl + V`)
   - 파일명을 `RefBoard-Setup-0.0.0.exe`로 변경 (공백을 하이픈으로 변경)

### CMD 사용

```bash
# refboard-website 리포지토리로 이동
cd D:\Scripts\refboard-website

# downloads 폴더 생성
mkdir downloads

# 설치 파일 복사 및 이름 변경
copy "D:\Scripts\RefBoard\release\RefBoard Setup 0.0.0.exe" "downloads\RefBoard-Setup-0.0.0.exe"
```

---

## 📤 3단계: 홈페이지 파일 복사

### Windows 탐색기 사용

1. **홈페이지 파일 복사**
   - `D:\Scripts\RefBoard\website\` 폴더 열기
   - 다음 파일들을 선택:
     - `index.html`
     - `style.css`
     - `privacy.html`
   - `Ctrl + C`로 복사

2. **리포지토리로 이동**
   - `D:\Scripts\refboard-website\` 폴더 열기

3. **파일 붙여넣기**
   - 루트 폴더에 `Ctrl + V`로 붙여넣기
   - 기존 파일이 있으면 덮어쓰기 확인

### CMD 사용

```bash
# refboard-website 리포지토리로 이동
cd D:\Scripts\refboard-website

# 홈페이지 파일 복사
copy D:\Scripts\RefBoard\website\index.html index.html
copy D:\Scripts\RefBoard\website\style.css style.css
copy D:\Scripts\RefBoard\website\privacy.html privacy.html
```

---

## 🚀 4단계: Git 커밋 및 푸시

```bash
# refboard-website 리포지토리로 이동
cd D:\Scripts\refboard-website

# 변경사항 확인
git status

# 모든 변경사항 추가
git add .

# 커밋
git commit -m "Add Windows installer download and restore homepage"

# 푸시
git push origin main
```

---

## ✅ 완료 확인

배포 완료 후 (약 1-2분 소요):

1. **홈페이지 확인**
   - `https://trackk81.github.io/refboard-website/index.html`
   - "Windows 설치 파일 다운로드" 버튼이 표시되어야 합니다

2. **다운로드 테스트**
   - 버튼을 클릭하면 `RefBoard-Setup-0.0.0.exe` 파일이 다운로드되어야 합니다
   - 다운로드 URL: `https://trackk81.github.io/refboard-website/downloads/RefBoard-Setup-0.0.0.exe`

---

## 📁 최종 구조

```
refboard-website/
├── index.html                    ← 홈페이지 메인
├── style.css                     ← 홈페이지 스타일
├── privacy.html                  ← 개인정보처리방침
├── downloads/                     ← 다운로드 폴더
│   └── RefBoard-Setup-0.0.0.exe ← Windows 설치 파일
└── ...
```

---

## 🆘 문제 해결

### 문제 1: 다운로드가 안 됨

**원인:** 파일 경로가 잘못되었을 수 있습니다.

**해결:**
- `downloads` 폴더가 루트에 있는지 확인
- 파일명이 정확한지 확인 (`RefBoard-Setup-0.0.0.exe`)
- GitHub Pages에서 파일이 업로드되었는지 확인

### 문제 2: 404 에러

**원인:** 파일이 업로드되지 않았을 수 있습니다.

**해결:**
- `git status`로 파일이 추가되었는지 확인
- `git add downloads/` 실행
- `git commit` 및 `git push` 실행

### 문제 3: 파일명에 공백이 있어서 문제

**해결:**
- 파일명을 `RefBoard-Setup-0.0.0.exe`로 변경 (공백을 하이픈으로)
- `index.html`의 링크도 동일하게 수정

---

## 📝 체크리스트

- [ ] 홈페이지 파일 복사 (`index.html`, `style.css`, `privacy.html`)
- [ ] `downloads` 폴더 생성
- [ ] 설치 파일 복사 및 이름 변경 (`RefBoard-Setup-0.0.0.exe`)
- [ ] Git 커밋 및 푸시 완료
- [ ] 홈페이지 확인 (`https://trackk81.github.io/refboard-website/index.html`)
- [ ] 다운로드 버튼 클릭 테스트

---

## 🎉 완료!

모든 단계를 완료하면:
- 홈페이지가 정상적으로 표시됩니다
- "Windows 설치 파일 다운로드" 버튼이 작동합니다
- 사용자가 설치 파일을 다운로드할 수 있습니다



